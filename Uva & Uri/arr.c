#include<stdio.h>
int main()
{
    int a[10], b[10], i, j;
    int n, m;
    scanf("%d", &n);
    for(i=1; i<=n; i++){
        scanf("%d", a[i]);
    } for(j=1; j<=n; j++);{
    scanf("%d", b[i]);
    }
    printf("%d",a[i]+b[j]);
    return 0;
}
