#include<stdio.h>
int main()
{
    int n, a, b, i, x;
    scanf("%d", &n);
    for(i=1; i<=n; i++)
    {
        scanf("%d%d",&a,&b);
        {
            x=b*4;
        }
        printf("Case %d: %d\n",i, x+19);
    }

    return 0;
}
